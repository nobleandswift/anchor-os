<%= form_for(@html_link) do |f| %>
  <% if @html_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@html_link.errors.count, "error") %> prohibited this html_link from being saved:</h2>

      <ul>
      <% @html_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
